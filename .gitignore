# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Streamlit
.streamlit/secrets.toml

# Generated files
*.png
!photos/*.png
# frozen matplotlib figures must be committed for CI
!_freeze/**/*.png
# curated site assets are tracked (charts/GIFs generated from the blobsim
# package — see assets/images/blobsim/PROVENANCE.md — then committed so they
# deploy; the *.png rule above must not swallow them)
!assets/**

# Build output
_site/
.playwright-mcp/

# Dev-only, not needed to render the deployed pages (kept locally):
#   data/      — raw NYC inputs (44 MB)
#   analysis/  — NYC analysis helpers, used only to regenerate the _freeze cache
/data/
/analysis/

.claude/
.serena/

# Quarto
/.quarto/
**/*.quarto_ipynb*

**/*.quarto_ipynb
